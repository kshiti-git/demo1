# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest' # ✅ This uses Microsoft-hosted agent

steps:
- script: echo "✅ Hello, your pipeline is running using Microsoft-hosted agent"
  displayName: "Run echo command"